%h2 Without kind
%ol#without_kind
  - @unknown_kind.each do |p|
    %li
      = link_to p.subject, admin_post_path(p)
      %br
      acum
      = time_ago_in_words(p.sent_date)
      
      .kinds
        - Post::KINDS.each do |kind|
          - form_for p, :url => admin_post_path(p) do |f|
            = f.hidden_field :kind, :value => kind
            = submit_tag kind

%h2 Unmatched posts
%ol
- @unmatched.each do |p|
  %li
    %strong
      = p.kind
    = link_to p.subject, admin_post_path(p)
    %br
    acum
    = time_ago_in_words(p.sent_date)
